# Netlify deployment configuration.
#
# The app is a Vite SPA that talks to /api/* endpoints. On AI Studio those endpoints
# are served by the Express app in server.ts, but Netlify only hosts static assets +
# serverless functions, so /api/* is routed to the Netlify Functions in netlify/functions.
#
# IMPORTANT: DAVID's current UI/one-writer architecture is assembled by the prebuild
# patch chain. Do not bypass npm run prebuild here or production will silently ship the
# old cockpit/Consult build while CI audits a different source tree.
#
# Required environment variable (Site configuration -> Environment variables):
#   GEMINI_API_KEY

[build]
  command = "npm run prebuild && vite build"
  publish = "dist"
  functions = "netlify/functions"

[functions]
  node_bundler = "esbuild"

# Map the app's /api/* calls onto the serverless functions.
[[redirects]]
  from = "/api/consult"
  to = "/.netlify/functions/consult"
  status = 200
  force = true

[[redirects]]
  from = "/api/synthesize"
  to = "/.netlify/functions/synthesize"
  status = 200
  force = true

[[redirects]]
  from = "/api/simulate-target"
  to = "/.netlify/functions/simulate-target"
  status = 200
  force = true

[[redirects]]
  from = "/api/health"
  to = "/.netlify/functions/health"
  status = 200
  force = true

# SPA fallback - must stay last so it does not swallow the /api/* rules above.
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
